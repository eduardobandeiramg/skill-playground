import 'package:flutter/material.dart';

class Navigator2RouteDelegate extends StatelessWidget {
  const Navigator2RouteDelegate({super.key});

  @override
  Widget build(BuildContext context) {
    return Navigator(pages: [],);
  }
}
